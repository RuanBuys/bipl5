% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_biplot.R
\name{scale_mds}
\alias{scale_mds}
\alias{scale_mds.bipl5_spec}
\title{Scale a biplot specification into a bipl5_biplot}
\usage{
scale_mds(x, type = c("pca", "cva", "pco", "regress"), ...)

\method{scale_mds}{bipl5_spec}(x, type = c("pca", "cva", "pco", "regress"), ...)
}
\arguments{
\item{x}{A \code{bipl5_spec} created by \code{\link[=init_biplot]{init_biplot()}}.}

\item{type}{The biplot method to construct. One of \code{"pca"}, \code{"cva"},
\code{"pco"}, \code{"regress"}, or \code{"regression"}.}

\item{...}{Additional named arguments for the chosen method.}
}
\value{
A fully formed \code{bipl5_biplot}.
}
\description{
\code{scale_mds()} turns a \code{bipl5_spec} created by \code{\link[=init_biplot]{init_biplot()}} into a fully
formed \code{bipl5_biplot} by dispatching to one of the underlying
\code{\link[biplotEZ:PCA]{biplotEZ::PCA()}}, \code{\link[biplotEZ:CVA]{biplotEZ::CVA()}}, \code{\link[biplotEZ:PCO]{biplotEZ::PCO()}}, or
\code{\link[biplotEZ:regress]{biplotEZ::regress()}} methods and then compiling only the requested
\code{mdsDisplay}. Any additional displays can be added later with
\code{\link[=append_mdsDisplay]{append_mdsDisplay()}}.
}
\details{
The \code{type} argument chooses the underlying biplot method. Additional
arguments are method-specific and should be supplied via \code{...}.

Supported aliases in \code{...}:
\itemize{
\item Common: \code{classes}, \code{group_aes} / \code{group.aes}, \code{title} / \code{Title}
\item PCA: \code{dimensions} / \code{dim.biplot}, \code{eigenvectors} / \code{e.vects},
\code{show_class_means} / \code{show.class.means} / \code{show_group_means} /
\code{show.group.means}, \code{correlation_biplot} / \code{correlation.biplot}
\item CVA: \code{classes}, \code{dimensions} / \code{dim.biplot}, \code{eigenvectors} / \code{e.vects},
\code{weighted_cva} / \code{weightedCVA}, \code{show_class_means} / \code{show.class.means} /
\code{show_group_means} / \code{show.group.means}, \code{low_dim} / \code{low.dim}
\item PCO: \code{Dmat} / \code{dist_mat}, \code{dist_func} / \code{dist.func},
\code{dist_func_cat} / \code{dist.func.cat}, \code{dimensions} / \code{dim.biplot},
\code{eigenvectors} / \code{e.vects}, \code{show_class_means} / \code{show.class.means} /
\code{show_group_means} / \code{show.group.means}, \code{axes}
\item \code{regress}: \code{Z} / \code{z}, \code{show_group_means} / \code{show.group.means} /
\code{show_class_means} / \code{show.class.means}, \code{axes}
}

For \code{type = "pco"}, any remaining named arguments in \code{...} are forwarded to
the chosen distance function.
}
